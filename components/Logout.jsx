import { useSession, signOut } from "next-auth/react"

export default function Logout(props) {
   // const { data: session } = useSession()
   // if (session) {
   //    return (
   //       <span>
   //          <button onClick={() => signOut()} className={props.calsses}>
   //             Sign out
   //          </button>
   //       </span>
   //    )
   // }
   return null;
}